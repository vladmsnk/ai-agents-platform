FROM golang:1.25-alpine AS build
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /mock-agent ./cmd/mock-agent

FROM alpine:3.20
RUN apk add --no-cache ca-certificates
COPY --from=build /mock-agent /mock-agent
ENTRYPOINT ["/mock-agent"]
